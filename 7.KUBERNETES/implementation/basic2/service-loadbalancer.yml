apiVersion: v1
kind: Service
metadata:
  name: welcome-lb
spec:
  type: LoadBalancer
  selector:
    app: welcome # same as the pod lebal name
  ports:
    - port: 80  
      targetPort: 8080 # run in pod